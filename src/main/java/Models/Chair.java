package Models;

public class Chair extends Space{
    public Chair(String spaceId, String spaceName, int spaceCapacity, boolean isReserved) {
        super(spaceId, spaceName, spaceCapacity, isReserved);
    }
}
